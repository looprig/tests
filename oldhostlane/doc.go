// Package oldhostlane documents the build-only compatibility module. The
// executable in cmd/oldhost is intentionally available only with the
// integration and kind build tags, and its published dependency graph is
// deliberately older than the parent tests module.
package oldhostlane
